# chess.com client engine bundle — third-party copyrighted code, not ours to
# redistribute. Keep locally for reverse-engineering/testing only.
research/variants.js
research/variants.pretty.js

# local git worktrees used for isolated feature work
.worktrees/
.claude/worktrees/

# scratch state from agent-driven development runs (task briefs, review diffs)
.superpowers/

# personal Claude Code settings; the shared ones are in .claude/settings.json
.claude/settings.local.json
CLAUDE.local.md

# Rust build artifacts
target/

# cargo-fuzz corpus/crash output for crates/core/fuzz -- its target/ dir is
# already covered by the blanket `target/` rule above.
crates/core/fuzz/corpus/
crates/core/fuzz/artifacts/

# wasm-pack output, rebuilt by CI on every deploy
web/pkg/
